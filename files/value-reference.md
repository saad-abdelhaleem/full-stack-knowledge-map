```js
const valueReferenceLesson = {
  id: "value-reference",
  moduleId: "runtime",
  title: { en: "Value vs reference semantics", ar: "دلالات القيمة مقابل المرجع" },
  summary: { en: "Why assigning one variable to another sometimes copies the data and sometimes shares it — and the silent bugs, hidden copies and allocations that follow from that one difference.", ar: "لماذا يَنسخ إسناد متغيّر إلى آخر البيانات أحياناً ويشاركها أحياناً أخرى — والأخطاء الصامتة والنسخ المخفية والتخصيصات الناتجة عن هذا الفرق الواحد." },
  mins: 17,
  sections: [
    {
      key: "why",
      blocks: [
        { t: "p", en: "Every type in C# is either a value type or a reference type. When you assign a value type to another variable, the data itself is copied, and you now have two independent things. When you assign a reference type, only the address is copied, and both variables point at the same single object. Almost everything else in this lesson follows from that one difference.", ar: "كل type في C# إمّا value type أو reference type. عند إسناد value type إلى متغيّر آخر تُنسخ البيانات نفسها، فيصبح لديك شيئان مستقلان. وعند إسناد reference type يُنسخ العنوان فقط، فيشير المتغيّران إلى نفس الكائن الواحد. وكل ما تبقّى في هذا الدرس تقريباً ينبع من هذا الفرق الواحد." },
        { t: "kv", rows: [
          { k: { en: "Value type", ar: "Value type" }, v: { en: "A type whose variable holds the data itself. Assigning or passing it copies the data. `struct`, `enum`, `int`, `decimal`, `bool`, `DateTime` are value types.", ar: "type يحمل متغيّره البيانات نفسها. إسناده أو تمريره ينسخ البيانات. `struct` و`enum` و`int` و`decimal` و`bool` و`DateTime` كلها value types." } },
          { k: { en: "Reference type", ar: "Reference type" }, v: { en: "A type whose variable holds only the address of an object that lives somewhere else. Assigning it copies the address, not the object. `class`, `record`, `interface`, arrays and `string` are reference types.", ar: "type يحمل متغيّره العنوان فقط لكائن يعيش في مكان آخر. إسناده ينسخ العنوان لا الكائن. `class` و`record` و`interface` والمصفوفات و`string` كلها reference types." } },
          { k: { en: "Reference", ar: "Reference" }, v: { en: "The address. A small number — 8 bytes on a 64-bit machine — that tells the runtime where the object sits in memory.", ar: "العنوان. رقم صغير — 8 bytes على جهاز 64-bit — يخبر الـ runtime أين يقع الكائن في الذاكرة." } },
          { k: { en: "Shallow copy", ar: "نسخ سطحي" }, v: { en: "Copying a value copies its fields one by one. If one of those fields is a reference, only the address is copied — the object behind it is still shared by both copies.", ar: "نسخ قيمة ينسخ حقولها واحداً واحداً. وإذا كان أحد الحقول reference فالمنسوخ هو العنوان فقط — والكائن خلفه يبقى مشتركاً بين النسختين." } },
          { k: { en: "Mutation", ar: "Mutation" }, v: { en: "Changing a field of something that already exists, instead of building a new thing with the new field.", ar: "تغيير حقل في شيء موجود أصلاً، بدل بناء شيء جديد بالحقل الجديد." } },
          { k: { en: "Defensive copy", ar: "نسخة دفاعية" }, v: { en: "A hidden copy the compiler makes to protect a value it promised would not change. It costs time on every call and it silently throws away any mutation.", ar: "نسخة مخفية ينشئها الـ compiler لحماية قيمة وعد بأنها لن تتغيّر. تكلّف وقتاً في كل نداء وتُلقي بأي mutation في سلة المهملات بصمت." } }
        ]},
        { t: "p", en: "Think of a value type as a photocopy. You hand a colleague a photocopy of a price list, then scribble a discount on your sheet. Their sheet is untouched, because they hold a different piece of paper. Now think of a reference type as a key to a room. You hand your colleague a copy of the key to the room where the one real price list is pinned to the wall. What got copied is the key, not the list — so when either of you writes on that sheet, both of you see the change.", ar: "تخيّل الـ value type نسخة مصوَّرة. تعطي زميلك نسخة مصوَّرة من قائمة أسعار ثم تكتب خصماً على ورقتك. ورقته تبقى كما هي لأنه يحمل ورقة أخرى. والآن تخيّل الـ reference type مفتاح غرفة. تعطي زميلك نسخة من مفتاح الغرفة التي عُلّقت فيها قائمة الأسعار الحقيقية الوحيدة على الحائط. المنسوخ هنا هو المفتاح لا القائمة — فإذا كتب أحدكما على تلك الورقة رأى الاثنان التغيير." },
        { t: "p", en: "The split exists because the two kinds of data have very different costs. An `int` stored as an object would need a 16-byte header, its own slot on the heap, and a pointer chase every time you read it — for 4 bytes of actual data. A list of a million points would be a million separate allocations for the garbage collector to track. So the runtime keeps small, short-lived, single-value data inline where it is used, and gives full object treatment only to things that have an identity worth sharing.", ar: "هذا الانقسام موجود لأن نوعَي البيانات مختلفان جداً في الكلفة. لو خُزّن `int` ككائن لاحتاج header بحجم 16 bytes، ومكاناً خاصاً به على الـ heap، ومتابعة مؤشّر في كل قراءة — مقابل 4 bytes من البيانات الفعلية. وقائمة من مليون نقطة ستصبح مليون تخصيص منفصل يتتبّعها الـ garbage collector. لذلك يُبقي الـ runtime البيانات الصغيرة قصيرة العمر ذات القيمة الواحدة في مكان استعمالها مباشرة، ويمنح معاملة الكائن الكاملة لما له هوية تستحق المشاركة." },
        { t: "callout", kind: "note", en: "`string` is a reference type, but you can never change the characters inside an existing string, so in everyday code it behaves like a value: nothing you do through one variable can change what another variable sees. That comes from immutability, not from value semantics. Do not use `string` as your mental model for how structs behave.", ar: "`string` هو reference type، لكن لا يمكنك تغيير الحروف داخل string موجود، فيتصرّف في الكود اليومي مثل القيمة: لا شيء تفعله عبر متغيّر يغيّر ما يراه متغيّر آخر. وهذا ناتج عن الـ immutability لا عن دلالات القيمة. لا تجعل `string` نموذجك الذهني لسلوك الـ structs." }
      ]
    },
    {
      key: "problem",
      blocks: [
        { t: "p", en: "Here is the one example this whole lesson uses. A checkout endpoint recalculates a cart: `POST /carts/{id}/reprice`. Each line is a `CartLine` holding a quantity, a unit price and a discount. Someone declared `CartLine` as a `struct`, because it is just data. The reprice loop reads each line out of a `List<CartLine>`, sets `Discount`, and returns the new total. It passed review, it passed tests, and in production every single discount was ignored — with a 200 response, no exception, and nothing in the logs.", ar: "هذا هو المثال الوحيد الذي يستعمله الدرس كله. endpoint للدفع يعيد حساب سلّة: `POST /carts/{id}/reprice`. كل سطر هو `CartLine` يحمل الكمية وسعر الوحدة والخصم. أعلن أحدهم `CartLine` كـ`struct` لأنه «مجرّد بيانات». حلقة إعادة التسعير تقرأ كل سطر من `List<CartLine>` وتضبط `Discount` ثم تُعيد المجموع الجديد. مرّت المراجعة ومرّت الاختبارات، وفي الإنتاج تُجوهل كل خصم — مع رد 200 وبلا exception وبلا أي سطر في الـ logs." },
        { t: "p", en: "The cause is one line: `var line = lines[i];`. On a `List<CartLine>`, the indexer is a method that returns the struct by value, so `line` is a fresh copy of the data. Setting `line.Discount = 0.1m` changes that copy, and the copy is discarded when the loop iteration ends. The list still holds the original untouched line. The exact same code with `CartLine` declared as a `class` would have worked, because then `lines[i]` returns the address of the one and only line object.", ar: "السبب سطر واحد: `var line = lines[i];`. في `List<CartLine>` الـ indexer دالة تُعيد الـ struct بالقيمة، فيكون `line` نسخة جديدة من البيانات. وضبط `line.Discount = 0.1m` يغيّر تلك النسخة، والنسخة تُرمى عند انتهاء دورة الحلقة. والقائمة تبقى تحمل السطر الأصلي دون مساس. ونفس الكود تماماً لو أُعلن `CartLine` كـ`class` لعمل بشكل صحيح، لأن `lines[i]` حينها تُعيد عنوان الكائن الوحيد لذلك السطر." },
        { t: "kv", rows: [
          { k: { en: "Symptom", ar: "العَرَض" }, v: { en: "Discounts silently ignored. HTTP 200, correct-looking response shape, no error, no log entry.", ar: "خصومات مُتجاهَلة بصمت. HTTP 200، وشكل رد يبدو صحيحاً، وبلا خطأ وبلا سطر في الـ logs." } },
          { k: { en: "Blast radius", ar: "حجم الأثر" }, v: { en: "4,100 orders over 6 days billed at full price — roughly $26,000 charged that should not have been charged.", ar: "4,100 order خلال 6 أيام حُوسبت بالسعر الكامل — نحو 26,000 دولار حُصّلت دون وجه حق." } },
          { k: { en: "Why tests missed it", ar: "لماذا فاتت الاختبارات" }, v: { en: "The test asserted on the total the method returned, computed from the same copy. The copy was correct; the list was not. Nobody asserted on the list.", ar: "اختبر الاختبار المجموع الذي أعادته الدالة، وهو محسوب من نفس النسخة. النسخة كانت صحيحة والقائمة لم تكن. ولم يفحص أحد القائمة." } },
          { k: { en: "Second, quieter cost", ar: "الكلفة الثانية الأهدأ" }, v: { en: "`CartLine` had grown to 56 bytes. It was passed by value through 4 pricing helpers per line; at 200 lines that is 44 KB of pure copying per request, doing no work.", ar: "كبر `CartLine` حتى صار 56 bytes. ويُمرَّر بالقيمة عبر 4 دوال تسعير لكل سطر؛ وعند 200 سطر يعني ذلك 44 KB نسخاً خالصاً لكل request دون أي عمل مفيد." } },
          { k: { en: "The one-line fix", ar: "الإصلاح بسطر واحد" }, v: { en: "Either write `lines[i] = line;` after mutating the copy, or make `CartLine` a `class`. Both compile; they mean different things and have different costs.", ar: "إما كتابة `lines[i] = line;` بعد تعديل النسخة، أو جعل `CartLine` كـ`class`. كلاهما يُصرَّف؛ لكنهما يعنيان أمرين مختلفين وبكلفتين مختلفتين." } }
        ]},
        { t: "p", en: "There is a detail here worth noticing, because it explains why nobody caught it. Writing `lines[i].Discount = 0.1m` directly is a compile error on a `List<T>` — the compiler says you cannot modify the return value, which is it telling you the exact truth. Writing the same statement on a `CartLine[]` array compiles and works correctly, because array indexing gives back the element itself rather than a copy. So one statement is an error, a silent no-op, or perfectly correct depending only on which container holds the struct. The developer hit the compile error, worked around it with a local variable, and turned a loud error into a silent bug.", ar: "هنا تفصيلة تستحق الانتباه لأنها تفسّر لماذا لم ينتبه أحد. كتابة `lines[i].Discount = 0.1m` مباشرة خطأ تصريف في `List<T>` — يقول الـ compiler إنك لا تستطيع تعديل القيمة المُعادة، وهو يقول الحقيقة بدقة. وكتابة نفس الجملة على مصفوفة `CartLine[]` تُصرَّف وتعمل بشكل صحيح، لأن الفهرسة في المصفوفة تُعيد العنصر نفسه لا نسخة منه. فالجملة الواحدة إما خطأ أو عملية بلا أثر أو صحيحة تماماً، ويحدّد ذلك نوع الحاوية التي تحمل الـ struct فقط. اصطدم المطوّر بخطأ التصريف فتحايل عليه بمتغيّر محلي، فحوّل خطأً صريحاً إلى bug صامت." }
      ]
    },
    {
      key: "internals",
      blocks: [
        { t: "p", en: "The whole mechanism comes down to two questions the compiler answers for every type: where does the data live, and what gets copied when you hand it around. For a value type the data lives inside whatever contains it — a stack slot for a local, a slot inside an array, a slot inside a parent object — and handing it around copies all its bytes. For a reference type the data always lives in one object on the heap, and handing it around copies only the 8-byte address of that object.", ar: "تعود الآلية كلها إلى سؤالين يجيب عنهما الـ compiler لكل type: أين تعيش البيانات، وما الذي يُنسخ عند تمريرها. في الـ value type تعيش البيانات داخل ما يحتويها — خانة على الـ stack لمتغيّر محلي، أو خانة داخل مصفوفة، أو خانة داخل كائن أب — وتمريرها ينسخ كل bytes البيانات. وفي الـ reference type تعيش البيانات دائماً في كائن واحد على الـ heap، وتمريرها ينسخ عنوان ذلك الكائن فقط بحجم 8 bytes." },
        { t: "kv", rows: [
          { k: { en: "Stack slot", ar: "خانة الـ stack" }, v: { en: "Space reserved inside the current method call for a local variable. It disappears when the method returns, at no cost — nothing to collect.", ar: "مساحة محجوزة داخل نداء الدالة الحالي لمتغيّر محلي. تختفي عند عودة الدالة بلا كلفة — لا شيء يُجمَع." } },
          { k: { en: "Heap object", ar: "كائن على الـ heap" }, v: { en: "A block on the managed heap holding the fields plus a 16-byte header. It stays alive until the garbage collector proves nothing points at it.", ar: "كتلة على الـ managed heap تحمل الحقول مع header بحجم 16 bytes. تبقى حيّة حتى يثبت الـ garbage collector أن لا شيء يشير إليها." } },
          { k: { en: "By-value copy", ar: "نسخ بالقيمة" }, v: { en: "The bytes of a struct are memcpy'd into the target slot. A 56-byte struct means 56 bytes moved, every assignment and every parameter.", ar: "تُنسخ bytes الـ struct عبر memcpy إلى الخانة الهدف. فـstruct بحجم 56 bytes يعني نقل 56 bytes في كل إسناد وكل parameter." } },
          { k: { en: "Defensive copy", ar: "نسخة دفاعية" }, v: { en: "Emitted by the compiler before calling a method on a value it must not let change. The method runs against the copy, so writes inside it are lost.", ar: "يولّدها الـ compiler قبل نداء دالة على قيمة يجب ألا تتغيّر. تعمل الدالة على النسخة، فتضيع الكتابات داخلها." } },
          { k: { en: "`ref` local / `ref` parameter", ar: "`ref` محلي / `ref` parameter" }, v: { en: "A variable that holds the address of a storage slot rather than a copy of it. Writing through it lands on the original.", ar: "متغيّر يحمل عنوان خانة تخزين بدل نسخة منها. والكتابة عبره تصل إلى الأصل." } },
          { k: { en: "`readonly struct`", ar: "`readonly struct`" }, v: { en: "A struct declared so no method can change its own fields. Because of that promise, the compiler stops emitting defensive copies for it.", ar: "struct مُعلن بحيث لا تستطيع أي دالة تغيير حقوله. وبسبب هذا الوعد يتوقّف الـ compiler عن توليد نسخ دفاعية له." } }
        ]},
        { t: "p", en: "Trace one reprice request with `CartLine` as a struct. The `List<CartLine>` holds a single backing array of 200 slots, each 56 bytes — one allocation of about 11 KB, and the line data sits inside it. Now the loop calls `lines[i]`, which is `List<T>.this[int]`, an ordinary method that returns `T`. Returning a struct from a method means copying it out, so 56 bytes land in the local `line`. You mutate the local. The method returns, the stack frame is thrown away, and the 11 KB array is exactly as it was. If `CartLine` were a class, the same array would hold 200 addresses of 8 bytes each, plus 200 separate heap objects — 201 allocations instead of 1 — and `lines[i]` would copy an address, so your mutation would land on the real object.", ar: "تتبّع request واحد لإعادة التسعير و`CartLine` هو struct. تحمل `List<CartLine>` مصفوفة داخلية واحدة من 200 خانة بحجم 56 bytes لكل خانة — تخصيص واحد بنحو 11 KB، وبيانات السطور تسكن داخله. ثم تنادي الحلقة `lines[i]` وهي `List<T>.this[int]`، دالة عادية تُعيد `T`. وإعادة struct من دالة تعني نسخه للخارج، فتحطّ 56 bytes في المتغيّر المحلي `line`. تعدّل المحلي، ثم تعود الدالة ويُرمى إطار الـ stack، وتبقى مصفوفة الـ 11 KB كما كانت تماماً. ولو كان `CartLine` كائن class لحملت نفس المصفوفة 200 عنوان بحجم 8 bytes لكل عنوان، مع 200 كائن منفصل على الـ heap — 201 تخصيصاً بدل 1 — ولنسخت `lines[i]` عنواناً، فتصل تعديلاتك إلى الكائن الحقيقي." },
        { t: "code", lang: "csharp", label: { en: "The same statement: error, silent no-op, or correct", ar: "نفس الجملة: خطأ أو عملية بلا أثر أو صحيحة" }, code: "struct CartLine { public int Qty; public decimal UnitPrice; public decimal Discount; }\n\nvar list  = new List<CartLine> { new CartLine { Qty = 2, UnitPrice = 10m } };\nvar array = new CartLine[]     { new CartLine { Qty = 2, UnitPrice = 10m } };\n\n// 1. Compile error CS1612: cannot modify the return value of List<T>.this[int].\n//    The compiler is telling you the truth - the thing you got back is a copy.\n// list[0].Discount = 0.1m;\n\n// 2. Compiles, runs, does nothing. 'copy' is a separate 56-byte CartLine.\nvar copy = list[0];\ncopy.Discount = 0.1m;\nConsole.WriteLine(list[0].Discount);   // 0.0   <-- the production bug\n\n// 3. Correct. Array indexing yields the element itself, not a copy of it.\narray[0].Discount = 0.1m;\nConsole.WriteLine(array[0].Discount);  // 0.1\n\n// 4. Correct with a List too, if you deliberately put the mutated copy back.\nvar line = list[0];\nline.Discount = 0.1m;\nlist[0] = line;\nConsole.WriteLine(list[0].Discount);   // 0.1" },
        { t: "p", en: "The second mechanism is the defensive copy, and it is invisible until you look for it. When the compiler holds a value it has promised will not change — a `readonly` field, an `in` parameter, a `static readonly` — and you call a method or a property on that value, it cannot prove the method leaves the fields alone. So it copies the value into a temporary first and calls the method on the temporary. The promise is kept, the call costs a full copy every single time, and any field the method changed is thrown away with the temporary. It is like a librarian who will not let you write in the reference copy, so she photocopies the page and hands you that: you get to scribble, and your scribbles go in the bin.", ar: "الآلية الثانية هي النسخة الدفاعية، وهي غير مرئية حتى تبحث عنها. حين يحمل الـ compiler قيمة وعد بأنها لن تتغيّر — حقل `readonly` أو parameter بـ`in` أو `static readonly` — وتنادي دالة أو property على تلك القيمة، لا يستطيع إثبات أن الدالة لن تمسّ الحقول. فينسخ القيمة إلى متغيّر مؤقت أولاً وينادي الدالة على المؤقت. يُحفظ الوعد، ويكلّف النداء نسخة كاملة في كل مرة، وأي حقل غيّرته الدالة يُرمى مع المؤقت. الأمر أشبه بأمينة مكتبة ترفض أن تكتب في النسخة المرجعية، فتصوّر لك الصفحة وتعطيك الصورة: تكتب عليها كما تشاء، وتذهب كتابتك إلى سلة المهملات." },
        { t: "p", en: "Declaring the struct as `readonly struct` removes the guesswork: no method inside it is allowed to change a field, the compiler knows it, and it stops emitting the copy. That is why `readonly struct` is the default shape for a value type in modern C#, and why `in` — which passes the address and promises not to write through it — only actually saves you a copy when the struct is `readonly`. Pass a non-readonly struct with `in` and you often trade one copy for a different one.", ar: "إعلان الـ struct كـ`readonly struct` يزيل التخمين: لا يُسمح لأي دالة داخله بتغيير حقل، ويعرف الـ compiler ذلك، فيتوقّف عن توليد النسخة. لهذا يكون `readonly struct` هو الشكل الافتراضي للـ value type في C# الحديثة، ولهذا لا يوفّر `in` — الذي يمرّر العنوان ويعِد بعدم الكتابة عبره — نسخة فعلياً إلا إذا كان الـ struct من نوع `readonly`. فتمرير struct غير readonly بـ`in` كثيراً ما يستبدل نسخة بأخرى." },
        { t: "code", lang: "csharp", label: { en: "ref, in, out and a ref local into the list itself", ar: "ref و in و out ومتغيّر ref يشير داخل القائمة" }, code: "// readonly struct: no method may change a field, so no defensive copies are emitted.\nreadonly struct Money\n{\n    public readonly decimal Amount;\n    public Money(decimal amount) => Amount = amount;\n    public Money Plus(Money other) => new Money(Amount + other.Amount); // returns new, never mutates\n}\n\n// in  = pass the address, promise not to write through it. No 56-byte copy per call.\nstatic decimal Total(in CartLine line) => line.Qty * (line.UnitPrice - line.Discount);\n\n// ref = pass the address of the caller's own variable; writes land on the original.\nstatic void ApplyDiscount(ref CartLine line, decimal pct) => line.Discount = line.UnitPrice * pct;\n\n// out = same as ref, except the method must assign before it returns.\nstatic bool TryParseQty(string raw, out int qty) => int.TryParse(raw, out qty);\n\n// A ref local points straight into the list's backing array: no copy, no write-back.\nforeach (ref var l in CollectionsMarshal.AsSpan(list))\n    l.Discount = l.UnitPrice * 0.1m;" },
        { t: "p", en: "One more piece completes the picture: records. A `record class` is still a reference type, so assignment still copies an address — but the compiler generates `Equals` and `GetHashCode` that compare field by field, so two different objects with the same field values count as equal. A `record struct` is a value type that gets the same generated equality. `readonly record struct` is a value type, immutable, with generated value equality and no defensive copies — which is what you usually want for a small value concept like `Money`. The `with` expression on any record makes a new instance with some fields changed, and that copy is shallow: a `List<T>` field is shared between the old and the new instance.", ar: "قطعة أخيرة تُكمل الصورة: الـ records. الـ`record class` يبقى reference type، فالإسناد ما زال ينسخ عنواناً — لكن الـ compiler يولّد `Equals` و`GetHashCode` تقارنان حقلاً بحقل، فيُعدّ كائنان مختلفان بنفس قيم الحقول متساويين. و`record struct` هو value type يحصل على نفس المساواة المولَّدة. و`readonly record struct` هو value type غير قابل للتعديل، بمساواة قيمة مولَّدة وبلا نسخ دفاعية — وهو ما تريده عادةً لمفهوم قيمة صغير مثل `Money`. وتعبير `with` على أي record ينشئ نسخة جديدة ببعض الحقول المغيَّرة، وهذا النسخ سطحي: حقل من نوع `List<T>` يبقى مشتركاً بين النسخة القديمة والجديدة." }
      ]
    },
    {
      key: "tradeoffs",
      blocks: [
        { t: "tradeoff",
          pros: { en: [
            "A struct stores its data inline, so 200 of them in an array cost one allocation instead of 201.",
            "No allocation means no work for the garbage collector, so struct-heavy loops do not add GC pauses.",
            "Inline data sits next to its neighbours in memory, so scanning an array of structs reads far fewer cache lines.",
            "Value equality on a `record` or `record struct` is generated for you and is usually what a value concept means."
          ], ar: [
            "الـ struct يخزّن بياناته inline، فـ200 منه في مصفوفة تكلّف تخصيصاً واحداً بدل 201.",
            "غياب التخصيص يعني غياب العمل عن الـ garbage collector، فالحلقات المليئة بالـ structs لا تضيف توقّفات GC.",
            "البيانات الـ inline تجلس بجوار جيرانها في الذاكرة، فمسح مصفوفة structs يقرأ عدداً أقل بكثير من cache lines.",
            "مساواة القيمة في `record` أو `record struct` تُولَّد لك، وهي عادةً المعنى المقصود لمفهوم قيمة."
          ]},
          cons: { en: [
            "Every assignment, parameter and return copies all the bytes; above roughly 24 bytes that copying starts to cost more than an allocation would.",
            "Mutating a struct you pulled out of a collection changes a copy and silently does nothing.",
            "A non-`readonly` struct held in a `readonly` field gets a hidden defensive copy on every method call.",
            "Storing a struct in an `object`, an interface variable, or a non-generic collection boxes it — a heap allocation plus a copy."
          ], ar: [
            "كل إسناد وparameter وقيمة مُعادة ينسخ كل الـ bytes؛ وفوق نحو 24 bytes يبدأ هذا النسخ يكلّف أكثر من تخصيص.",
            "تعديل struct أخرجته من مجموعة يغيّر نسخة ولا يفعل شيئاً بصمت.",
            "struct غير `readonly` محفوظ في حقل `readonly` يحصل على نسخة دفاعية مخفية في كل نداء دالة.",
            "تخزين struct في `object` أو في متغيّر interface أو في مجموعة غير generic يعمل له boxing — تخصيص على الـ heap مع نسخة."
          ]},
          limits: { en: [
            "A struct can never be `null` unless you wrap it in `Nullable<T>`, which adds a bool field and its own copy cost.",
            "Structs cannot inherit from another struct or class, so no shared base behaviour.",
            "A struct with reference fields still copies only the addresses — the shared objects behind them are not copied.",
            "Value equality generated by `record` compares reference fields by reference, so two records holding equal-but-distinct lists are not equal."
          ], ar: [
            "لا يمكن للـ struct أن يكون `null` إلا بتغليفه في `Nullable<T>`، وهذا يضيف حقل bool وكلفة نسخ خاصة به.",
            "الـ structs لا ترث من struct أو class آخر، فلا سلوك أساسي مشترك.",
            "الـ struct الذي يحمل حقول reference ينسخ العناوين فقط — والكائنات المشتركة خلفها لا تُنسخ.",
            "مساواة القيمة المولَّدة في `record` تقارن حقول الـ reference بالمرجع، فسجلّان يحملان قائمتين متساويتَي المحتوى ومختلفتَي الهوية ليسا متساويين."
          ]},
          alts: { en: [
            "`class` — the default. Choose it whenever the thing has an identity, is large, or needs to be mutated in place.",
            "`readonly record struct` — the modern default for a small immutable value such as `Money`, `Sku` or `Percentage`.",
            "`record class` — a reference type with value equality, good for DTOs and query results you compare rather than mutate.",
            "`class` with private setters and `With...` methods — value-like behaviour without any of the struct copy rules."
          ], ar: [
            "`class` — الخيار الافتراضي. اخترْه متى كان للشيء هوية أو كان كبيراً أو احتاج تعديلاً في مكانه.",
            "`readonly record struct` — الافتراضي الحديث لقيمة صغيرة غير قابلة للتعديل مثل `Money` أو `Sku` أو `Percentage`.",
            "`record class` — reference type بمساواة قيمة، مناسب للـ DTOs ونتائج الاستعلامات التي تقارنها بدل تعديلها.",
            "`class` بـ setters خاصة ودوال `With...` — سلوك شبيه بالقيمة دون أي من قواعد النسخ الخاصة بالـ structs."
          ]}
        }
      ]
    },
    {
      key: "mistakes",
      blocks: [
        { t: "mistake",
          title: { en: "Mutating a struct you pulled out of a collection", ar: "تعديل struct أخرجته من مجموعة" },
          body: { en: "The reprice bug. The developer wrote `lines[i].Discount = 0.1m;`, hit compile error CS1612, and worked around it by copying into a local first. The local was a fresh 56-byte copy, the discount was applied to it, and the copy was discarded at the end of the iteration. Six days and 4,100 orders later someone noticed the revenue numbers. The compile error was the language warning you correctly; the workaround silenced it.", ar: "هذا هو bug إعادة التسعير. كتب المطوّر `lines[i].Discount = 0.1m;` فاصطدم بخطأ التصريف CS1612، فتحايل عليه بالنسخ إلى متغيّر محلي أولاً. كان المحلي نسخة جديدة بحجم 56 bytes، طُبّق الخصم عليها، ورُميت النسخة عند نهاية الدورة. وبعد ستة أيام و4,100 order لاحظ أحدهم أرقام الإيرادات. كان خطأ التصريف تحذيراً صحيحاً من اللغة، وأسكته التحايل." },
          fix: "// Option A: put the mutated copy back deliberately.\nvar line = lines[i];\nline.Discount = line.UnitPrice * 0.1m;\nlines[i] = line;\n\n// Option B (better): a ref local writes straight into the list's backing array.\nforeach (ref var l in CollectionsMarshal.AsSpan(lines))\n    l.Discount = l.UnitPrice * 0.1m;\n\n// Option C (best here): CartLine has identity and is mutated - make it a class."
        },
        { t: "mistake",
          title: { en: "A mutable struct in a readonly field, called in a hot loop", ar: "struct قابل للتعديل في حقل readonly يُنادى في حلقة ساخنة" },
          body: { en: "The pricing service held `private readonly TaxTable _table;` where `TaxTable` was a 200-byte struct. Every call to `_table.RateFor(sku)` made a defensive copy first, because the compiler could not prove `RateFor` leaves the fields alone. That is 200 bytes copied per lookup, 200 lookups per request — 40 KB of copying per request that does no work. p99 latency, meaning the slowest 1 request in 100, was 34 ms; after adding one keyword it was 19 ms. Nothing else changed.", ar: "احتفظت خدمة التسعير بـ`private readonly TaxTable _table;` حيث `TaxTable` هو struct بحجم 200 bytes. وكل نداء لـ`_table.RateFor(sku)` كان يصنع نسخة دفاعية أولاً، لأن الـ compiler لا يستطيع إثبات أن `RateFor` لا تمسّ الحقول. أي 200 bytes منسوخة لكل بحث، و200 بحث لكل request — 40 KB نسخاً لكل request بلا عمل مفيد. كان p99 للـ latency، أي أبطأ request من كل 100، يساوي 34 ms؛ وبعد إضافة كلمة مفتاحية واحدة صار 19 ms. ولم يتغيّر شيء آخر." },
          fix: "// One keyword. Now no method inside may change a field, so no defensive copy.\nreadonly struct TaxTable\n{\n    private readonly decimal[] _rates;\n    public decimal RateFor(string sku) => _rates[Bucket(sku)];\n}"
        },
        { t: "mistake",
          title: { en: "Assuming a copy is deep", ar: "افتراض أن النسخة عميقة" },
          body: { en: "A snapshot type was declared `record class CartSnapshot(int Version, List<CartLine> Lines)`. The code took an audit snapshot with `var before = snapshot with { Version = snapshot.Version + 1 };`, then repriced. Both `before` and `snapshot` pointed at the same `List<CartLine>` object, because `with` copies field values, and the value of a `List<T>` field is an address. The audit trail showed the after-state in both columns, so the diff was always empty and a real mispricing went unnoticed for a month.", ar: "أُعلن نوع اللقطة كـ`record class CartSnapshot(int Version, List<CartLine> Lines)`. أخذ الكود لقطة تدقيق بـ`var before = snapshot with { Version = snapshot.Version + 1 };` ثم أعاد التسعير. كان `before` و`snapshot` يشيران إلى نفس كائن `List<CartLine>`، لأن `with` ينسخ قيم الحقول، وقيمة حقل من نوع `List<T>` هي عنوان. فأظهر سجل التدقيق حالة «بعد» في العمودين، فبقي الفرق فارغاً دائماً ومرّ خطأ تسعير حقيقي دون ملاحظة شهراً كاملاً." },
          fix: "// Copy the contents too, not just the address.\nvar before = snapshot with { Lines = new List<CartLine>(snapshot.Lines) };\n\n// Or hold an immutable collection so sharing is safe by construction.\npublic sealed record CartSnapshot(int Version, ImmutableArray<CartLine> Lines);"
        },
        { t: "mistake",
          title: { en: "Comparing structs with the default Equals", ar: "مقارنة structs بـEquals الافتراضي" },
          body: { en: "A deduplication step used `if (!seen.Contains(line))` over a `List<CartLine>`. A plain struct has no `==` operator and no `IEquatable<T>`, so the runtime falls back to `ValueType.Equals`, which compares field by field using reflection whenever the struct contains a reference field — `CartLine` had a `string Sku`. Each comparison also boxed both sides, allocating twice. With 200 lines that is 20,000 reflection-based comparisons and 40,000 allocations per request. p99 went from 30 ms to 900 ms and the fix was one word in the declaration.", ar: "استعملت خطوة إزالة التكرار `if (!seen.Contains(line))` على `List<CartLine>`. والـ struct العادي بلا معامل `==` وبلا `IEquatable<T>`، فيعود الـ runtime إلى `ValueType.Equals` التي تقارن حقلاً بحقل عبر الـ reflection كلما احتوى الـ struct حقل reference — وكان في `CartLine` حقل `string Sku`. وكل مقارنة تعمل boxing للطرفين أيضاً، أي تخصيصان. ومع 200 سطر يعني ذلك 20,000 مقارنة قائمة على الـ reflection و40,000 تخصيص لكل request. ارتفع p99 من 30 ms إلى 900 ms، وكان الإصلاح كلمة واحدة في الإعلان." },
          fix: "// record struct generates a typed Equals and GetHashCode - no reflection, no boxing.\nreadonly record struct CartLine(string Sku, int Qty, decimal UnitPrice, decimal Discount);"
        }
      ]
    },
    {
      key: "interview",
      blocks: [
        { t: "qa", level: "junior",
          q: { en: "What is the difference between a value type and a reference type?", ar: "ما الفرق بين value type و reference type؟" },
          a: { en: "A value type variable holds the data itself, so when you assign it to another variable you get a full copy and the two are independent from then on. A reference type variable holds only the address of an object, so assigning it copies the address and both variables now point at the same object — change it through one and you see the change through the other. `struct`, `enum` and the built-in numbers are value types; `class`, `record`, arrays and `string` are reference types.", ar: "متغيّر الـ value type يحمل البيانات نفسها، فإسناده إلى متغيّر آخر يعطيك نسخة كاملة ويصبح الاثنان مستقلّين بعدها. ومتغيّر الـ reference type يحمل عنوان الكائن فقط، فإسناده ينسخ العنوان ويشير المتغيّران إلى نفس الكائن — غيّره عبر أحدهما فترى التغيير عبر الآخر. `struct` و`enum` والأرقام المدمجة هي value types؛ و`class` و`record` والمصفوفات و`string` هي reference types." } },
        { t: "qa", level: "mid",
          q: { en: "I pass a `List<CartLine>` into a method. If the method adds an item, does the caller see it? What if the method assigns a whole new list to the parameter?", ar: "أمرّر `List<CartLine>` إلى دالة. إذا أضافت الدالة عنصراً فهل يراه المنادي؟ وماذا لو أسندت الدالة قائمة جديدة كاملة إلى الـ parameter؟" },
          a: { en: "Adding an item is visible to the caller, because both the caller's variable and the parameter hold the same address, and `Add` changes the object at that address. Assigning a new list is not visible, because the parameter is its own variable holding its own copy of the address; overwriting it just points the local at something else. People call this pass-by-value of a reference. If you actually want the caller's variable repointed, you need `ref List<CartLine>`.", ar: "إضافة عنصر يراها المنادي، لأن متغيّر المنادي والـ parameter يحملان نفس العنوان، و`Add` تغيّر الكائن عند ذلك العنوان. وإسناد قائمة جديدة لا يراه، لأن الـ parameter متغيّر مستقل يحمل نسخته من العنوان؛ والكتابة فوقه توجّه المتغيّر المحلي إلى شيء آخر فقط. يسمّى هذا تمرير المرجع بالقيمة. وإن أردت فعلاً إعادة توجيه متغيّر المنادي فأنت بحاجة إلى `ref List<CartLine>`." } },
        { t: "qa", level: "mid",
          q: { en: "When would you actually choose a struct over a class?", ar: "متى تختار struct بدل class فعلياً؟" },
          a: { en: "When the thing is genuinely one value rather than an entity: `Money`, `Sku`, a coordinate, a percentage. Practically I check four things — it is small, around 16 to 24 bytes; it is immutable, so I can mark it `readonly struct`; it has no identity, meaning two instances with the same fields are interchangeable; and I create a lot of them, so avoiding an allocation each time matters. If any of those is false I use a class. A 56-byte mutable line item that gets edited in place fails three of the four.", ar: "حين يكون الشيء قيمة واحدة فعلاً لا كياناً: `Money` أو `Sku` أو إحداثي أو نسبة مئوية. عملياً أفحص أربعة أمور — أن يكون صغيراً بنحو 16 إلى 24 bytes؛ وغير قابل للتعديل حتى أعلنه `readonly struct`؛ وبلا هوية، أي أن نسختين بنفس الحقول قابلتان للتبادل؛ وأن أنشئ منه أعداداً كبيرة فيهمّني تفادي تخصيص في كل مرة. وإن اختلّ أي شرط استعملت class. وسطر فاتورة قابل للتعديل بحجم 56 bytes يُحرَّر في مكانه يفشل في ثلاثة من الأربعة." } },
        { t: "qa", level: "senior",
          q: { en: "What is a defensive copy, when does the compiler make one, and how do you get rid of it?", ar: "ما النسخة الدفاعية، ومتى يصنعها الـ compiler، وكيف تتخلّص منها؟" },
          a: { en: "It is a hidden copy the compiler inserts before calling a method on a struct it promised not to change — a `readonly` field, an `in` parameter, a `static readonly`. It cannot prove the method leaves the fields alone, so it copies first and calls on the copy. Two consequences: you pay a full struct copy on every call, and any mutation inside the method silently disappears. The fix is to declare the type `readonly struct`, or mark the individual member `readonly`, which gives the compiler the proof it needs. Note that `in` on a non-readonly struct can therefore make things slower, not faster.", ar: "هي نسخة مخفية يُدرجها الـ compiler قبل نداء دالة على struct وعد بألّا يتغيّر — حقل `readonly` أو parameter بـ`in` أو `static readonly`. لا يستطيع إثبات أن الدالة لا تمسّ الحقول، فينسخ أولاً وينادي على النسخة. والنتيجتان: تدفع نسخة struct كاملة في كل نداء، وأي تعديل داخل الدالة يختفي بصمت. والحل إعلان النوع `readonly struct`، أو وسم العضو المعنيّ بـ`readonly`، فيحصل الـ compiler على الإثبات المطلوب. ولاحظ أن `in` على struct غير readonly قد يجعل الأداء أبطأ لا أسرع." } },
        { t: "qa", level: "senior",
          q: { en: "`class`, `record class`, `record struct`, `readonly record struct` — how do you pick?", ar: "`class` و`record class` و`record struct` و`readonly record struct` — كيف تختار؟" },
          a: { en: "I ask two questions. First, does this thing have an identity? An `Order` row is still the same order after you change its status, so it needs reference equality and it is a `class`. A `Money` of 10 USD is interchangeable with any other 10 USD, so it wants value equality. Second, is it small and immutable? If yes, `readonly record struct` — no allocation, generated typed equality, no defensive copies. If it wants value equality but is large or holds collections, `record class`. And I never give an EF Core entity record equality: the change tracker relies on reference identity, and two structurally equal instances of different rows will confuse it.", ar: "أسأل سؤالين. أولاً، هل لهذا الشيء هوية؟ صف الـ`Order` يبقى نفس الـ order بعد تغيير حالته، فيحتاج مساواة بالمرجع ويكون `class`. و`Money` بقيمة 10 دولارات قابل للتبادل مع أي 10 دولارات أخرى، فيريد مساواة قيمة. ثانياً، هل هو صغير وغير قابل للتعديل؟ إن كان كذلك فـ`readonly record struct` — بلا تخصيص، ومساواة مولَّدة ومحدّدة النوع، وبلا نسخ دفاعية. وإن أراد مساواة قيمة لكنه كبير أو يحمل مجموعات فـ`record class`. ولا أمنح كياناً في EF Core مساواة الـ records أبداً: فالـ change tracker يعتمد على هوية المرجع، ونسختان متساويتان بنيوياً من صفّين مختلفين ستربكانه." } },
        { t: "qa", level: "staff",
          q: { en: "Your team has shipped three production bugs in a year that all trace back to struct copy semantics. What do you change, beyond fixing the code?", ar: "شحن فريقك ثلاثة أخطاء إنتاج في سنة تعود كلها إلى دلالات نسخ الـ structs. ما الذي تغيّره غير إصلاح الكود؟" },
          a: { en: "I would treat it as a defaults problem, not a knowledge problem, because these bugs are silent and reviewers cannot reliably spot them by reading. Three moves. First, a written rule with one line: a struct is `readonly` or it is a class — and turn on the analyzers that enforce it, so the build fails instead of a reviewer having to notice. Second, remove the reason people write their own value types: ship one shared `Money`, `Quantity` and `Sku` in a small kernel package that is already correct, so nobody rolls their own for the fourth time. Third, change what tests assert: each of these bugs passed because the test checked the return value and not the collection that was supposed to change, so I would add that to the test guidelines and to the postmortem template. I would also make one of the incidents a fifteen-minute internal talk, because a real revenue number teaches this faster than any doc.", ar: "سأعامل الأمر كمشكلة إعدادات افتراضية لا كمشكلة معرفة، لأن هذه الأخطاء صامتة ولا يستطيع المراجعون رصدها بالقراءة بشكل موثوق. ثلاث خطوات. أولاً، قاعدة مكتوبة بسطر واحد: الـ struct إمّا `readonly` وإمّا يكون class — مع تفعيل الـ analyzers التي تفرضها، ليفشل الـ build بدل انتظار انتباه مراجع. ثانياً، إزالة السبب الذي يدفع الناس لكتابة value types خاصة بهم: أطلق `Money` و`Quantity` و`Sku` مشتركة في حزمة صغيرة صحيحة أصلاً، فلا يكتبها أحد للمرة الرابعة. ثالثاً، تغيير ما تفحصه الاختبارات: مرّ كل bug من هذه لأن الاختبار فحص القيمة المُعادة لا المجموعة التي كان يفترض أن تتغيّر، فسأضيف ذلك إلى إرشادات الاختبار وإلى قالب الـ postmortem. وسأحوّل إحدى الحوادث إلى جلسة داخلية من خمس عشرة دقيقة، لأن رقم إيرادات حقيقي يعلّم هذا أسرع من أي مستند." } }
      ]
    },
    {
      key: "codereview",
      blocks: [
        { t: "review", severity: "high",
          title: { en: "A public mutable struct with a method that changes its own fields", ar: "struct عام قابل للتعديل بدالة تغيّر حقوله" },
          bad: "public struct CartLine\n{\n    public string Sku;\n    public int Qty;\n    public decimal UnitPrice;\n    public decimal Discount;\n\n    // Looks fine. Silently does nothing for half its callers.\n    public void ApplyDiscount(decimal pct) => Discount = UnitPrice * pct;\n}\n\n// caller\nforeach (var line in lines)      // 'line' is a copy of each element\n    line.ApplyDiscount(0.1m);    // the copy is discounted, then thrown away",
          good: "public readonly record struct CartLine(string Sku, int Qty, decimal UnitPrice, decimal Discount)\n{\n    // Returns a new value instead of changing this one. Impossible to lose.\n    public CartLine WithDiscount(decimal pct) => this with { Discount = UnitPrice * pct };\n}\n\n// caller\nfor (var i = 0; i < lines.Count; i++)\n    lines[i] = lines[i].WithDiscount(0.1m);",
          why: { en: "A mutating method on a mutable struct is a trap with no compiler warning. Called on a copy — a `foreach` variable, a `List<T>` indexer result, an `in` parameter, a `readonly` field — it changes something that is about to be discarded, and the code reads as if it worked. Making the type `readonly` turns that whole class of bug into a compile error, and returning a new value makes the caller state explicitly where the result should go.", ar: "الدالة التي تعدّل struct قابل للتعديل فخّ بلا أي تحذير من الـ compiler. فإذا نوديت على نسخة — متغيّر `foreach` أو نتيجة indexer في `List<T>` أو parameter بـ`in` أو حقل `readonly` — غيّرت شيئاً على وشك الرمي، ويبدو الكود كأنه عمل. وجعل النوع `readonly` يحوّل هذا الصنف كله من الأخطاء إلى خطأ تصريف، وإعادة قيمة جديدة تجبر المنادي على تحديد وجهة النتيجة صراحةً." }
        },
        { t: "review", severity: "medium",
          title: { en: "A large struct passed by value on a hot path", ar: "struct كبير يُمرَّر بالقيمة على مسار ساخن" },
          bad: "// CartLine is 56 bytes. Four helpers per line, 200 lines per request.\nstatic decimal LineTotal(CartLine line)   => line.Qty * (line.UnitPrice - line.Discount);\nstatic decimal LineTax(CartLine line)     => LineTotal(line) * 0.15m;\nstatic bool     IsFreeShip(CartLine line) => LineTotal(line) > 50m;",
          good: "// 'in' passes the 8-byte address instead of 56 bytes of data.\n// It only avoids a copy because CartLine is a readonly struct.\nstatic decimal LineTotal(in CartLine line)   => line.Qty * (line.UnitPrice - line.Discount);\nstatic decimal LineTax(in CartLine line)     => LineTotal(in line) * 0.15m;\nstatic bool     IsFreeShip(in CartLine line) => LineTotal(in line) > 50m;",
          why: { en: "Each by-value call copies all 56 bytes. Four calls per line across 200 lines is 44 KB of copying per request that produces nothing. Measured over 10 million calls, by-value took 41 ms and `in` took 12 ms — about 4 nanoseconds per call down to 1.2. Two conditions before you do this: the struct must be `readonly`, otherwise `in` just moves the copy to the callee side as a defensive copy; and the struct must actually be large, because below about 16 bytes passing the address costs more than passing the data.", ar: "كل نداء بالقيمة ينسخ الـ 56 bytes كاملة. وأربعة نداءات لكل سطر عبر 200 سطر تعني 44 KB نسخاً لكل request بلا أي ناتج. وبالقياس على 10 ملايين نداء استغرق التمرير بالقيمة 41 ms واستغرق `in` مقدار 12 ms — أي من نحو 4 نانوثانية لكل نداء إلى 1.2. وهناك شرطان قبل فعل ذلك: أن يكون الـ struct من نوع `readonly`، وإلا نقل `in` النسخة إلى جهة المُنادى كنسخة دفاعية؛ وأن يكون الـ struct كبيراً فعلاً، لأنه دون نحو 16 bytes يكلّف تمرير العنوان أكثر من تمرير البيانات." }
        }
      ]
    },
    {
      key: "sysdesign",
      blocks: [
        { t: "p", en: "At system level the sharpest edge is not the struct — it is the shared reference. An in-process cache such as `IMemoryCache` hands every concurrent request the very same object instance. If one request handler mutates the cached `PricingRules` object to apply a customer-specific override, every other request in that process now sees that customer's override, and the bug appears as random wrong prices for unrelated customers under load. It also disappears on a single-request reproduction, which is why these take days to find.", ar: "على مستوى النظام تكون الحافة الأخطر ليست الـ struct بل المرجع المشترك. فذاكرة تخزين داخل العملية مثل `IMemoryCache` تعطي كل request متزامن نفس نسخة الكائن تماماً. وإذا عدّل معالج request واحد كائن `PricingRules` المخزّن ليطبّق تجاوزاً خاصاً بعميل، رأى كل request آخر في تلك العملية تجاوز ذلك العميل، فيظهر الـ bug كأسعار خاطئة عشوائية لعملاء لا علاقة لهم تحت الحمل. ويختفي أيضاً عند إعادة الإنتاج بـrequest واحد، ولهذا يستغرق العثور عليه أياماً." },
        { t: "p", en: "So the design rule is about ownership, not about `struct` versus `class`: for every object that crosses a boundary — a cache, a static field, a singleton service, a queue, a background job — decide whether the receiver owns it or borrows it, and make the type enforce that decision. Shared things should be immutable so borrowing is always safe. Owned things can be mutable, but then each caller must get its own instance.", ar: "فقاعدة التصميم تدور حول الملكية لا حول `struct` مقابل `class`: لكل كائن يعبر حدوداً — cache أو حقل static أو خدمة singleton أو طابور أو مهمة خلفية — حدّد هل يملكه المستقبِل أم يستعيره، واجعل النوع يفرض ذلك القرار. فالأشياء المشتركة ينبغي أن تكون غير قابلة للتعديل ليكون الاستعارة آمنة دائماً. والأشياء المملوكة يمكن أن تكون قابلة للتعديل، لكن على كل منادٍ عندئذٍ أن يأخذ نسخته الخاصة." },
        { t: "ul",
          en: [
            "Cached configuration and rules: store an immutable record; to change it, build a new instance and replace the cache entry, never edit in place.",
            "DTOs at the API boundary: `record class` gives value equality for free, which makes response comparison in tests and idempotency checks straightforward.",
            "Message payloads on a queue: whatever you send is serialized, so treat the in-memory object as immutable after publishing — a background handler holding the same instance is a real hazard.",
            "High-throughput parsing and serialization: small `readonly struct` readers over `Span<T>` keep the whole hot path allocation-free, which is where the struct choice really pays.",
            "Domain money and quantity types: a `readonly record struct Money(decimal Amount, string Currency)` makes it impossible to add USD to EUR by accident, and costs no allocation."
          ],
          ar: [
            "الإعدادات والقواعد المخزّنة: خزّن record غير قابل للتعديل؛ ولتغييره ابنِ نسخة جديدة واستبدل مدخل الـ cache، ولا تحرّره في مكانه أبداً.",
            "الـ DTOs عند حدود الـ API: `record class` يمنحك مساواة قيمة مجاناً، فتسهل مقارنة الردود في الاختبارات وفحوص الـ idempotency.",
            "حمولات الرسائل في الطوابير: ما ترسله يُسلسَل، فعامل الكائن في الذاكرة كغير قابل للتعديل بعد النشر — ومعالج خلفي يمسك نفس النسخة خطر حقيقي.",
            "التحليل والتسلسل عالي الإنتاجية: قارئات `readonly struct` صغيرة فوق `Span<T>` تُبقي المسار الساخن كله بلا تخصيص، وهنا يؤتي اختيار الـ struct ثماره فعلاً.",
            "أنواع المال والكمية في المجال: `readonly record struct Money(decimal Amount, string Currency)` يجعل جمع USD مع EUR بالخطأ مستحيلاً، وبلا أي تخصيص."
          ]
        },
        { t: "callout", kind: "warn", en: "Anything you put in a static field, a singleton, or a memory cache is shared by every request in the process at the same time. If it is mutable, you have created shared mutable state across threads — which is both a correctness bug and a race condition, and it will only show up under production concurrency.", ar: "كل ما تضعه في حقل static أو في singleton أو في memory cache يكون مشتركاً بين جميع الـ requests في العملية في الوقت نفسه. وإذا كان قابلاً للتعديل فقد أنشأت حالة مشتركة قابلة للتعديل عبر الـ threads — وهذا خطأ صحّة وحالة سباق معاً، ولن يظهر إلا تحت تزامن الإنتاج." }
      ]
    },
    {
      key: "perf",
      blocks: [
        { t: "kv", rows: [
          { k: { en: "Memory", ar: "الذاكرة" }, v: { en: "200 `CartLine` structs live inside one 11 KB backing array — one allocation. As a class it is 201 allocations: the array of 200 addresses plus 200 objects, each carrying a 16-byte header, about 15 KB total.", ar: "200 struct من `CartLine` تعيش داخل مصفوفة واحدة بحجم 11 KB — تخصيص واحد. وككائنات class تصبح 201 تخصيصاً: مصفوفة بـ200 عنوان مع 200 كائن يحمل كل منها header بحجم 16 bytes، بنحو 15 KB إجمالاً." } },
          { k: { en: "CPU", ar: "المعالج" }, v: { en: "Passing a 56-byte struct by value is a 56-byte memory copy per call. Ten million calls took 41 ms by value and 12 ms with `in` — about 4 nanoseconds each versus 1.2.", ar: "تمرير struct بحجم 56 bytes بالقيمة هو نسخ 56 bytes من الذاكرة لكل نداء. عشرة ملايين نداء استغرقت 41 ms بالقيمة و12 ms مع `in` — أي نحو 4 نانوثانية لكل نداء مقابل 1.2." } },
          { k: { en: "Latency", ar: "زمن الاستجابة" }, v: { en: "Structs used as locals produce no garbage, so they add nothing to GC pause time. Replacing 200 short-lived objects per request removed one Gen0 collection every ~90 requests and took p99 — the slowest 1 request in 100 — from 34 ms to 19 ms.", ar: "الـ structs المستعملة كمتغيّرات محلية لا تنتج نفايات، فلا تضيف شيئاً إلى زمن توقّف الـ GC. واستبدال 200 كائن قصير العمر لكل request أزال جمع Gen0 واحداً كل نحو 90 request وخفّض p99 — أبطأ request من كل 100 — من 34 ms إلى 19 ms." } },
          { k: { en: "Scalability", ar: "قابلية التوسّع" }, v: { en: "Allocation rate then scales with real work rather than with object count. At 2,000 requests per second that was about 400,000 fewer objects allocated every second, on the same hardware.", ar: "يصبح معدّل التخصيص متناسباً مع العمل الحقيقي لا مع عدد الكائنات. وعند 2,000 request في الثانية كان ذلك نحو 400,000 كائن أقل تخصيصاً كل ثانية، على نفس العتاد." } }
        ]}
      ]
    },
    {
      key: "debug",
      blocks: [
        { t: "ul",
          en: [
            "`sharplab.io` — paste the code and set the output to IL or to lowered C#. A defensive copy shows up as an extra temporary local and a `ldobj`/`stobj` pair right before the call you did not expect to copy anything.",
            "BenchmarkDotNet with `[MemoryDiagnoser]` — run the struct and class versions side by side and read the `Allocated` column. If it did not drop, the struct is being boxed somewhere and you have not saved anything.",
            "`dotnet-counters monitor --counters System.Runtime --process-id <pid>` — watch `alloc-rate` and `gen-0-gc-count`. If allocation rate is low but CPU is high, your cost is copying, not allocating.",
            "`Unsafe.SizeOf<CartLine>()` printed once at startup — the real size in bytes. Anything over about 24 bytes is worth questioning as a by-value parameter, and the number usually surprises people.",
            "The `ErrorProne.NET.Structs` Roslyn analyzer package — it warns on hidden copies of non-readonly structs and suggests `in` where it would help. Treating its warnings as build errors stops the whole class of bug at the PR."
          ],
          ar: [
            "`sharplab.io` — الصق الكود واضبط المخرجات على IL أو على C# المخفَّض. تظهر النسخة الدفاعية كمتغيّر مؤقت إضافي وزوج `ldobj`/`stobj` قبل النداء الذي لم تتوقّع أن ينسخ شيئاً.",
            "BenchmarkDotNet مع `[MemoryDiagnoser]` — شغّل نسخة الـ struct ونسخة الـ class جنباً إلى جنب واقرأ عمود `Allocated`. فإن لم ينخفض فالـ struct يخضع لـboxing في مكان ما ولم توفّر شيئاً.",
            "`dotnet-counters monitor --counters System.Runtime --process-id <pid>` — راقب `alloc-rate` و`gen-0-gc-count`. فإن كان معدّل التخصيص منخفضاً والمعالج مرتفعاً فكلفتك نسخ لا تخصيص.",
            "`Unsafe.SizeOf<CartLine>()` مطبوعاً مرة عند الإقلاع — الحجم الحقيقي بالـ bytes. وكل ما تجاوز نحو 24 bytes يستحق التساؤل كـparameter بالقيمة، والرقم يفاجئ الناس عادةً.",
            "حزمة الـ analyzer المسماة `ErrorProne.NET.Structs` — تحذّر من النسخ المخفية للـ structs غير الـ readonly وتقترح `in` حيث ينفع. ومعاملة تحذيراتها كأخطاء build توقف هذا الصنف كله من الأخطاء عند الـ PR." ]
        },
        { t: "callout", kind: "tip", en: "Before anyone argues struct versus class in a review, put two numbers on the screen: `Unsafe.SizeOf<T>()` and a BenchmarkDotNet run with `[MemoryDiagnoser]` showing both versions. Most of these debates end in thirty seconds once the size and the allocation column are visible, because the disagreement is almost always about a size nobody had actually measured.", ar: "قبل أن يجادل أحد حول struct مقابل class في مراجعة، ضع رقمين على الشاشة: `Unsafe.SizeOf<T>()` وتشغيلة BenchmarkDotNet مع `[MemoryDiagnoser]` تُظهر النسختين. تنتهي معظم هذه النقاشات في ثلاثين ثانية بمجرد ظهور الحجم وعمود التخصيص، لأن الخلاف يكون دائماً تقريباً حول حجم لم يقسه أحد فعلاً." }
      ]
    },
    {
      key: "realworld",
      blocks: [
        { t: "p", en: "Value semantics stop being a language detail and become an architectural choice in two situations: when you allocate millions of small things per second, and when a wrong copy quietly changes money. The first is a throughput problem and the second is a correctness problem, and they push you toward the same answer — small immutable value types with generated equality, and reference types reserved for things that genuinely have an identity.", ar: "تتوقّف دلالات القيمة عن كونها تفصيلة لغوية وتصبح قراراً معمارياً في حالتين: حين تخصّص ملايين الأشياء الصغيرة في الثانية، وحين تغيّر نسخة خاطئة مبلغاً مالياً بصمت. الأولى مشكلة إنتاجية والثانية مشكلة صحّة، وتدفعك الاثنتان إلى نفس الجواب — أنواع قيمة صغيرة غير قابلة للتعديل بمساواة مولَّدة، وأنواع مرجعية محفوظة لما له هوية حقيقية." },
        { t: "ul",
          en: [
            "Trading and market-data systems: price ticks arrive by the million per second, so they are modelled as small readonly structs written into pre-allocated buffers — one allocation at startup instead of millions per second.",
            "Payment and billing platforms: `Money` is a value type with the currency inside it, so the type system rejects adding two different currencies, and no cached amount can be mutated by another request.",
            "Game and simulation backends: positions and velocities live as structs in flat arrays, so the physics loop reads memory sequentially instead of following a pointer to a separate object per entity.",
            "Parsing and serialization libraries: the reader is a `ref struct` over a `Span<byte>` that cannot escape to the heap by construction, which is how these libraries parse large payloads with zero allocations."
          ],
          ar: [
            "أنظمة التداول وبيانات السوق: تصل نبضات الأسعار بالملايين في الثانية، فتُنمذَج كـstructs صغيرة readonly تُكتب في buffers مخصَّصة مسبقاً — تخصيص واحد عند الإقلاع بدل ملايين في الثانية.",
            "منصّات الدفع والفوترة: `Money` نوع قيمة يحمل العملة داخله، فيرفض نظام الأنواع جمع عملتين مختلفتين، ولا يستطيع request آخر تعديل مبلغ مخزّن.",
            "خوادم الألعاب والمحاكاة: تعيش المواقع والسرعات كـstructs في مصفوفات مسطّحة، فتقرأ حلقة الفيزياء الذاكرة تسلسلياً بدل تتبّع مؤشّر إلى كائن منفصل لكل كيان.",
            "مكتبات التحليل والتسلسل: القارئ هو `ref struct` فوق `Span<byte>` لا يستطيع الهروب إلى الـ heap بحكم بنيته، وهكذا تحلّل هذه المكتبات حمولات كبيرة بلا أي تخصيص."
          ]
        }
      ]
    },
    {
      key: "exercises",
      blocks: [
        { t: "ex", diff: "easy", en: "Write a small program with a `struct Point { public int X; }`. Put one into a `List<Point>` and one into a `Point[]`. Try `list[0].X = 5;` and `array[0].X = 5;`. You have got it right when you can state, without running it, which line fails to compile, what error it gives, and why the array line succeeds.", ar: "اكتب برنامجاً صغيراً فيه `struct Point { public int X; }`. ضع واحداً في `List<Point>` وواحداً في `Point[]`. جرّب `list[0].X = 5;` و`array[0].X = 5;`. تكون قد أتقنتها حين تستطيع أن تقول دون تشغيل أيّ السطرين يفشل في التصريف، وما الخطأ الذي يعطيه، ولماذا ينجح سطر المصفوفة." },
        { t: "ex", diff: "medium", en: "Take the reprice loop from this lesson and fix it three different ways: write the copy back with `lines[i] = line;`, use a `ref` local through `CollectionsMarshal.AsSpan`, and change `CartLine` to a class. Then write a test that fails on the original code. You have got it right when the test asserts on the list contents rather than on the returned total — that is the assertion the real team was missing.", ar: "خذ حلقة إعادة التسعير من هذا الدرس وأصلحها بثلاث طرق: أعِد كتابة النسخة بـ`lines[i] = line;`، واستعمل متغيّر `ref` عبر `CollectionsMarshal.AsSpan`، وحوّل `CartLine` إلى class. ثم اكتب اختباراً يفشل على الكود الأصلي. تكون قد أتقنتها حين يفحص الاختبار محتويات القائمة لا المجموع المُعاد — فهذا هو الفحص الذي كان ينقص الفريق الحقيقي." },
        { t: "ex", diff: "hard", en: "Build a 64-byte struct with a method that reads its fields. Call it ten million times from a `readonly` field, then again after adding the `readonly` keyword to the struct declaration. Measure both with BenchmarkDotNet, and open the same code in sharplab.io. You have got it right when the benchmark shows a clear gap and you can point at the extra temporary local in the IL that explains it.", ar: "ابنِ struct بحجم 64 bytes بدالة تقرأ حقوله. نادِه عشرة ملايين مرة من حقل `readonly`، ثم أعِد النداء بعد إضافة الكلمة `readonly` إلى إعلان الـ struct. قِس الحالتين بـBenchmarkDotNet، وافتح نفس الكود في sharplab.io. تكون قد أتقنتها حين يُظهر القياس فرقاً واضحاً وتستطيع أن تشير إلى المتغيّر المؤقت الإضافي في الـ IL الذي يفسّره." },
        { t: "ex", diff: "senior", en: "Audit one real service you own. List every type that is a struct and record its `Unsafe.SizeOf<T>()`, whether it is `readonly`, and whether anything mutates it. Then list every mutable object stored in a static field, a singleton, or `IMemoryCache`. You have got it right when you produce a one-page memo naming the two or three types that should change, with the measured cost or the concrete shared-mutation risk for each — not a list of everything that could theoretically be improved.", ar: "دقّق خدمة حقيقية واحدة تملكها. اسرد كل type هو struct وسجّل `Unsafe.SizeOf<T>()` له، وهل هو `readonly`، وهل يعدّله شيء. ثم اسرد كل كائن قابل للتعديل مخزّن في حقل static أو في singleton أو في `IMemoryCache`. تكون قد أتقنتها حين تنتج مذكّرة من صفحة واحدة تسمّي النوعين أو الثلاثة التي يجب تغييرها، مع الكلفة المقيسة أو خطر المشاركة الملموس لكل منها — لا قائمة بكل ما يمكن تحسينه نظرياً." }
      ]
    },
    {
      key: "refs",
      blocks: [
        { t: "ref", label: { en: "Structure types (C# language reference)", ar: "أنواع الـ struct (مرجع لغة C#)" }, url: "https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/builtin-types/struct", meta: { en: "Docs", ar: "توثيق" } },
        { t: "ref", label: { en: "Records (C# language reference)", ar: "الـ records (مرجع لغة C#)" }, url: "https://learn.microsoft.com/en-us/dotnet/csharp/language-reference/builtin-types/record", meta: { en: "Docs", ar: "توثيق" } },
        { t: "ref", label: { en: "Choosing between class and struct", ar: "الاختيار بين class و struct" }, url: "https://learn.microsoft.com/en-us/dotnet/standard/design-guidelines/choosing-between-class-and-struct", meta: { en: "Design guidelines", ar: "إرشادات تصميم" } },
        { t: "ref", label: { en: "SharpLab — see the IL and lowered C# your code compiles to", ar: "SharpLab — شاهد الـ IL وC# المخفَّض الناتج عن كودك" }, url: "https://sharplab.io/", meta: { en: "Tool", ar: "أداة" } }
      ]
    }
  ],
  quiz: [
    {
      q: { en: "`CartLine` is a struct. What does `var line = lines[0]; line.Discount = 0.1m;` do to a `List<CartLine>`?", ar: "`CartLine` هو struct. ماذا يفعل `var line = lines[0]; line.Discount = 0.1m;` بـ`List<CartLine>`؟" },
      options: [
        { en: "It sets the discount on the first element of the list.", ar: "يضبط الخصم على العنصر الأول في القائمة." },
        { en: "It sets the discount on a copy; the list is unchanged and no error is raised.", ar: "يضبط الخصم على نسخة؛ والقائمة لا تتغيّر ولا يُرفع أي خطأ." },
        { en: "It throws an InvalidOperationException at runtime.", ar: "يرمي InvalidOperationException أثناء التشغيل." },
        { en: "It fails to compile, because you cannot modify a struct pulled from a list.", ar: "يفشل في التصريف، لأنك لا تستطيع تعديل struct مأخوذ من قائمة." }
      ],
      correct: 1,
      why: { en: "`List<T>.this[int]` is a method that returns `T`, so returning a struct copies it out. `line` is an independent 56-byte copy; mutating it is perfectly legal and completely invisible to the list, which is why nothing throws and nothing is logged. The compile error people remember — CS1612 — happens only for the direct form `lines[0].Discount = 0.1m;`, and copying into a local is exactly the workaround that turns that loud error into a silent bug. The same code on a `CartLine[]` array would have worked, because array indexing yields the element itself rather than a copy.", ar: "`List<T>.this[int]` دالة تُعيد `T`، وإعادة struct تعني نسخه للخارج. فـ`line` نسخة مستقلة بحجم 56 bytes؛ وتعديلها قانوني تماماً وغير مرئي للقائمة إطلاقاً، ولهذا لا يُرمى شيء ولا يُسجَّل شيء. أما خطأ التصريف الذي يتذكّره الناس — CS1612 — فيحدث فقط في الصيغة المباشرة `lines[0].Discount = 0.1m;`، والنسخ إلى متغيّر محلي هو بالضبط التحايل الذي يحوّل ذلك الخطأ الصريح إلى bug صامت. ونفس الكود على مصفوفة `CartLine[]` كان سيعمل، لأن الفهرسة في المصفوفة تُعيد العنصر نفسه لا نسخة منه." }
    },
    {
      q: { en: "You pass a `List<string>` to a method. Inside, the method calls `list.Add(\"x\")` and then `list = new List<string>()`. What does the caller see afterwards?", ar: "تمرّر `List<string>` إلى دالة. وداخلها تنادي الدالة `list.Add(\"x\")` ثم `list = new List<string>()`. ماذا يرى المنادي بعد ذلك؟" },
      options: [
        { en: "The original list with \"x\" added to it.", ar: "القائمة الأصلية وقد أُضيف إليها \"x\"." },
        { en: "The new empty list.", ar: "القائمة الجديدة الفارغة." },
        { en: "The original list, unchanged.", ar: "القائمة الأصلية دون تغيير." },
        { en: "A copy of the original list with \"x\" added.", ar: "نسخة من القائمة الأصلية مع إضافة \"x\"." }
      ],
      correct: 0,
      why: { en: "The parameter is its own variable holding a copy of the address. `Add` follows that address and changes the one real list object, so the caller sees the new item. The assignment then overwrites only the parameter variable, pointing the callee's local at a different object; the caller's variable still holds the original address and knows nothing about it. This is why the phrase is pass-by-value of a reference: the reference is copied, the object is not. To repoint the caller's own variable you would need `ref List<string>`.", ar: "الـ parameter متغيّر مستقل يحمل نسخة من العنوان. و`Add` تتبع ذلك العنوان وتغيّر كائن القائمة الحقيقي الوحيد، فيرى المنادي العنصر الجديد. ثم يكتب الإسناد فوق متغيّر الـ parameter فقط، فيوجّه محلي المُنادى إلى كائن آخر؛ ومتغيّر المنادي ما زال يحمل العنوان الأصلي ولا يعلم بشيء. ولهذا تُسمّى العبارة تمرير المرجع بالقيمة: المرجع يُنسخ والكائن لا. ولإعادة توجيه متغيّر المنادي نفسه تحتاج `ref List<string>`." }
    },
    {
      q: { en: "Why does adding the `readonly` keyword to a struct declaration sometimes make code measurably faster, with no other change?", ar: "لماذا تجعل إضافة الكلمة `readonly` إلى إعلان struct الكودَ أسرع بشكل ملموس أحياناً، دون أي تغيير آخر؟" },
      options: [
        { en: "It moves the struct from the heap to the stack.", ar: "تنقل الـ struct من الـ heap إلى الـ stack." },
        { en: "It lets the JIT inline every method on the struct.", ar: "تتيح للـ JIT عمل inline لكل دوال الـ struct." },
        { en: "It proves no method can change a field, so the compiler stops emitting defensive copies.", ar: "تثبت أن لا دالة تستطيع تغيير حقل، فيتوقّف الـ compiler عن توليد النسخ الدفاعية." },
        { en: "It makes the struct immutable, so the runtime can cache and reuse instances.", ar: "تجعل الـ struct غير قابل للتعديل، فيستطيع الـ runtime تخزين النسخ وإعادة استعمالها." }
      ],
      correct: 2,
      why: { en: "When a struct is held in a `readonly` field, an `in` parameter, or a `static readonly`, the compiler must guarantee the value does not change. For a normal struct it cannot prove that a method leaves the fields alone, so it copies the whole struct into a temporary and calls the method on the temporary — one full copy per call. `readonly struct` makes the guarantee part of the type, so the proof exists and the copy is not emitted. Option A is wrong because `readonly` does not change where the data lives; a struct inside a heap object is still on the heap either way.", ar: "حين يكون الـ struct محفوظاً في حقل `readonly` أو parameter بـ`in` أو `static readonly`، يجب أن يضمن الـ compiler عدم تغيّر القيمة. وفي الـ struct العادي لا يستطيع إثبات أن الدالة لا تمسّ الحقول، فينسخ الـ struct كاملاً إلى متغيّر مؤقت وينادي الدالة عليه — نسخة كاملة لكل نداء. و`readonly struct` يجعل الضمان جزءاً من النوع، فيوجد الإثبات ولا تُولَّد النسخة. والخيار الأول خاطئ لأن `readonly` لا يغيّر مكان سكن البيانات؛ فالـ struct داخل كائن على الـ heap يبقى على الـ heap في الحالتين." }
    },
    {
      q: { en: "`record class CartSnapshot(int Version, List<CartLine> Lines)`. You write `var b = a with { Version = 2 };` then `b.Lines.Clear()`. What is the state of `a.Lines`?", ar: "`record class CartSnapshot(int Version, List<CartLine> Lines)`. تكتب `var b = a with { Version = 2 };` ثم `b.Lines.Clear()`. ما حالة `a.Lines`؟" },
      options: [
        { en: "Unchanged — `with` makes a deep copy of every field.", ar: "دون تغيير — `with` يصنع نسخة عميقة لكل حقل." },
        { en: "Also empty — both records point at the same list object.", ar: "فارغة أيضاً — كلا الـ records يشيران إلى نفس كائن القائمة." },
        { en: "Unchanged, because records are immutable.", ar: "دون تغيير، لأن الـ records غير قابلة للتعديل." },
        { en: "It throws, because you cannot mutate a collection inside a record.", ar: "يرمي خطأً، لأنك لا تستطيع تعديل مجموعة داخل record." }
      ],
      correct: 1,
      why: { en: "`with` copies field values, and the value of a `List<T>` field is an address. So `a` and `b` hold the same address and there is only one list object in memory; clearing it through `b` empties what `a` sees too. This is a shallow copy, and it is the same rule that applies to a struct holding a reference field. Records being immutable only means you cannot reassign `b.Lines` to a different list — it says nothing about the object that field points at. To get independence you must copy the contents yourself, `Lines = new List<CartLine>(a.Lines)`, or hold an immutable collection so sharing is safe.", ar: "`with` ينسخ قيم الحقول، وقيمة حقل من نوع `List<T>` هي عنوان. فـ`a` و`b` يحملان نفس العنوان ولا يوجد إلا كائن قائمة واحد في الذاكرة؛ وتفريغه عبر `b` يفرّغ ما يراه `a` أيضاً. هذا نسخ سطحي، وهو نفس القاعدة التي تنطبق على struct يحمل حقل reference. وكون الـ records غير قابلة للتعديل يعني فقط أنك لا تستطيع إعادة إسناد `b.Lines` إلى قائمة أخرى — ولا يقول شيئاً عن الكائن الذي يشير إليه الحقل. وللاستقلال عليك نسخ المحتويات بنفسك، `Lines = new List<CartLine>(a.Lines)`، أو حمل مجموعة غير قابلة للتعديل ليكون التشارك آمناً." }
    },
    {
      q: { en: "You need a `Money` type used millions of times per request loop, and an `Order` entity tracked by EF Core. What do you declare each as?", ar: "تحتاج نوع `Money` يُستعمل ملايين المرات في حلقة كل request، وكياناً `Order` يتتبّعه EF Core. بماذا تعلن كلاً منهما؟" },
      options: [
        { en: "`Money` as a `readonly record struct`, `Order` as a `class`.", ar: "`Money` كـ`readonly record struct`، و`Order` كـ`class`." },
        { en: "Both as `record class`, for value equality everywhere.", ar: "كلاهما `record class`، لمساواة قيمة في كل مكان." },
        { en: "Both as structs, to avoid allocations.", ar: "كلاهما structs، لتفادي التخصيصات." },
        { en: "`Money` as a `class` so it can be null, `Order` as a `record struct`.", ar: "`Money` كـ`class` ليكون قابلاً لأن يكون null، و`Order` كـ`record struct`." }
      ],
      correct: 0,
      why: { en: "`Money` is a small value concept: no identity, immutable, never meaningfully null, created in huge numbers. That is exactly the `readonly record struct` case — no allocation, generated typed equality instead of the slow reflection-based `ValueType.Equals`, and no defensive copies. `Order` is an entity: it has identity, so the same order must stay distinguishable from an equal-looking one. Giving it record value equality actively breaks EF Core, whose change tracker relies on reference identity to know which tracked object maps to which row. Option C is wrong for `Order` on size alone — a large aggregate passed by value is copy cost with nothing saved.", ar: "`Money` مفهوم قيمة صغير: بلا هوية، وغير قابل للتعديل، ولا يكون null بمعنى مفيد، ويُنشأ بأعداد ضخمة. وهذه بالضبط حالة `readonly record struct` — بلا تخصيص، وبمساواة مولَّدة محدّدة النوع بدل `ValueType.Equals` البطيء القائم على الـ reflection، وبلا نسخ دفاعية. و`Order` كيان: له هوية، فيجب أن يبقى الـ order نفسه مميّزاً عن آخر يشبهه. ومنحه مساواة قيمة بأسلوب الـ records يكسر EF Core فعلياً، إذ يعتمد الـ change tracker على هوية المرجع ليعرف أي كائن متتبَّع يقابل أي صف. والخيار الثالث خاطئ لـ`Order` لسبب الحجم وحده — فaggregate كبير يُمرَّر بالقيمة كلفة نسخ بلا أي توفير." }
    }
  ]
};
```

NEXT: boxing
